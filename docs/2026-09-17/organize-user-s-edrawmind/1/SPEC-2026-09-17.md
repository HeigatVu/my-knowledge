# Spec: Organize the "Model Evaluation and Selection" EdrawMind mindmap into a Cornell knowledge note

## Assumptions

A1. The workspace is the `my-knowledge` Obsidian vault (this git worktree, branch `claude/model-eval-obsidian-notes-ef1273`), and the deliverable is a markdown note inside it, not application code.
A2. The vault already has an established convention for this exact kind of task, visible in sibling notes under `AI_ML/` (e.g. `03_ML-Lifecycle/03_Diagnostics-and-Error-Analysis.md`, `03_ML-Lifecycle/01_Project-Scoping-and-Dataset.md`, `02_Machine-Learning/01_ML-Fundamentals-and-Terminology.md`, `00_AI-Engineer-MOC.md`): Cornell-style frontmatter (`cssclasses: [cornell-left, cornell-border]`, `priority`, `order`, `topic-group`, `gist`, `aliases`, `tags`), a `> [!summary] Summary` block, a `## 📌 Original EdrawMind Mindmap & Note` section that preserves the user's raw outline verbatim inside a `> [!quote]` block, then `> [!cue] What is it?`, `> [!cue] Why is it important?`, `> [!cue] How is it related to ...?`. This spec follows that convention rather than inventing a new one, since consistency with the existing vault outweighs strict adherence to the Templater file's variable-prompt mechanics (this run is non-interactive, so Templater's `tp.system.prompt`/`tp.system.suggester` calls are answered by inference from context instead of an interactive prompt).
A3. `10_System/Templates/Cornell-Note (Templater).md` is the structural reference for the note shape (summary callout, `What is it? / Why is it important? / How is it related to ...?` cues). It is not copied verbatim because the sibling notes already establish a superset of it (adding the "Original Mindmap" section, tags, gist) that better serves "relate to previous knowledge."
A4. "Keeping my original note" means the full raw EdrawMind outline text the user pasted is preserved verbatim (structure, indentation, in-line notes, typos included) inside the note, not paraphrased away.
A5. Many leaf items in the user's mindmap (e.g. most regression-metric families: Mean Error, MAPE, sMAPE, MRAE, GMRAE, RelMAE, RSE, MASE, RMSSE, PB, RMSLE) are outline headers only ("How to calculate / Example / Pros and cons") with no content filled in yet. The synthesized "What/Why/How" sections elaborate in depth only on branches where the user's mindmap already has substantive notes (e.g. MSE's 1/2 factor, the log-loss coin-toss example, KL divergence, BCE's entropy intuition, hinge loss vs. exponential loss, label smoothing, multi-label sigmoid+CE, the confusion-matrix Vietnamese aside, the F1 "why not average precision/recall" example, triplet loss, contrastive loss, and the bias-variance / error-analysis discussion). The still-empty leaves are organized into a compact index/table so the outline stays navigable without fabricating formulas or pros/cons the user has not written yet.
A6. This is a personal knowledge-base repo, not a software product: there is no test runner, linter, or CI configured (`vibe-agent doctor` reports no `vibe-checks.yaml`). Verifier nodes on the delivery graph that read that file are expected to skip; "testing strategy" below is `file_assert`-style structural checks, not executable tests.
A7. `AI_ML/04_Model-Evaluation-and-Selection/01_Model-Evaluation-and-Selection.md` already exists as an **untracked** file in the main checkout of this same repo (sibling working tree, not this worktree, not committed to any branch) with a placeholder mindmap-quote block ("this section serves as a foundational placeholder"). That file belongs to a different, uncommitted editing session against the shared vault and is left untouched. This run creates the equivalent, fully-populated note as a tracked file on its own branch in this worktree, to be reviewed and merged like the vault's other topic branches (`claude/data-collection-preprocessing-*`, `claude/math-knowledge-restructure-*`, `claude/signal-processing-notes-review-*` siblings show this is the vault's normal workflow). The PR description will flag the untracked main-checkout file so whoever merges reconciles or discards it deliberately instead of losing work silently.
A8. Out of scope: touching the other AI_ML sibling notes' content, or `00_AI-Engineer-MOC.md`, beyond reading them for cross-link accuracy. Those files are shared across the sibling topic branches above; editing them here would create needless merge conflicts across concurrently in-flight PRs for a one-line benefit.

→ Correct now or I proceed.

## Objective

Turn the user's EdrawMind outline on "Model Evaluation and Selection" into one Cornell-format Obsidian note at `AI_ML/04_Model-Evaluation-and-Selection/01_Model-Evaluation-and-Selection.md`, cross-linked to the related notes already in the vault, with the original outline preserved verbatim.

## Tech stack

- Obsidian vault (markdown + YAML frontmatter), Templater community plugin conventions for the Cornell template.
- Git for version control (this repo); no build tooling, package manager, or CI is configured for vault content.

## Commands

None (no build/test commands apply to a documentation-only vault). Verification is by reading the resulting file and confirming structure/content, plus standard `git` commands for diffing and committing.

## Project structure (this workspace)

```
AI_ML/
  00_AI-Engineer-MOC.md                          # shared index, not touched
  02_Machine-Learning/01_ML-Fundamentals-and-Terminology.md   # linked, not touched
  03_ML-Lifecycle/01_Project-Scoping-and-Dataset.md           # linked, not touched
  03_ML-Lifecycle/03_Diagnostics-and-Error-Analysis.md        # linked, not touched
  04_Model-Evaluation-and-Selection/
    01_Model-Evaluation-and-Selection.md         # THIS deliverable (new file in this branch)
10_System/Templates/Cornell-Note (Templater).md  # structural reference (read-only)
docs/2026-09-17/organize-user-s-edrawmind/1/     # this run's SPEC/PLAN/TASKS artifacts
```

## Code style

Match the sibling notes' exact section order and callout syntax:
`frontmatter` → `> [!summary] Summary` → `## 📌 Original EdrawMind Mindmap & Note` (with `> [!quote] Mindmap Source Content`) → `> [!cue] What is it?` → `> [!cue] Why is it important?` → `> [!cue] How is it related to ...?`. Use `[[wikilink]]` syntax for cross-references, and Mermaid/tables the same way the sibling notes do for structure-heavy content.

## Testing strategy

No executable tests exist for this workspace. Verification is structural (`file_assert`):

1. The target file exists at the path above and parses as valid YAML frontmatter + markdown.
2. It contains all five required sections in order (Summary, Original Mindmap, What, Why, How).
3. The "Original EdrawMind Mindmap & Note" quote block contains the full raw outline text supplied by the user, not a placeholder or a paraphrase.
4. Every named metric/loss/technique from the user's outline appears somewhere in the note (as a detailed subsection or in the compact index table for not-yet-elaborated leaves) — nothing from the original outline is silently dropped.
5. `[[wikilink]]`-style cross-references resolve to note titles that exist elsewhere in the vault (even if not present in this branch's working tree, per A7/A8).
6. No unrelated file under `AI_ML/` or `10_System/` is modified.

## Boundaries

- **Always**: preserve the user's original mindmap text verbatim; follow the vault's existing Cornell-note convention; cross-link to genuinely related existing notes.
- **Ask first**: nothing in this run — the objective is unambiguous given the established sibling convention.
- **Never**: delete or rewrite unrelated notes; invent detailed formulas/pros-cons for outline leaves the user never filled in; edit the untracked main-checkout stub file (A7); commit secrets (none present in scope).

## Data classification

The content is the user's personal, non-sensitive study notes (public ML concepts). No credentials, tokens, or personal data are touched. Nothing here is restricted from appearing anywhere in the repo.

## Success criteria

- [ ] `AI_ML/04_Model-Evaluation-and-Selection/01_Model-Evaluation-and-Selection.md` exists on this branch, Cornell-formatted, with the full original mindmap preserved.
- [ ] What/Why/How sections cover every branch of the mindmap (elaborated where the user wrote detail, indexed where they did not).
- [ ] Cross-links to `01_Project-Scoping-and-Dataset`, `03_Diagnostics-and-Error-Analysis`, and `01_ML-Fundamentals-and-Terminology` are present and accurate (no duplicated bias-variance theory that already lives in `03_Diagnostics-and-Error-Analysis`).
- [ ] Change is committed on this branch and a PR is opened against `main`, noting the untracked main-checkout file for the human merger.

## Open questions

None.
